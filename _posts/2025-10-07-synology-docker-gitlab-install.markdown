# install
default username = root

set root password by add env `GITLAB_ROOT_PASSWORD = root_password`

https://zaki-hmkc.hatenablog.com/entry/2024/01/31/224926

# git clone url

```
git remote set-url origin ssh://git@server-ip:port/username/repo_name.git
```

username = root
port = docker's contaner 22's local port(ex:33356)
